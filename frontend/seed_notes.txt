const { createClient } = require('@supabase/supabase-js');
const fs = require('fs');
const path = require('path');
require('dotenv').config({ path: '.env.local' });

// We need to parse ts file if we can, but since it's mockSeedData.ts which is a TS file containing
// the JS object, we can't easily require it in plain node without compilation.
// However, I can write a small next.js api route and call it.
